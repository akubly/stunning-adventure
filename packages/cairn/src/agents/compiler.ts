/**
 * Compiler Agent
 *
 * BYO plugin validator and builder. Supports build mode (compile + validate)
 * and audit mode (security + compatibility checks).
 *
 * Placeholder — implementation in a future phase.
 */

export const AGENT_NAME = 'compiler';
export const AGENT_DESCRIPTION = 'BYO plugin validator + builder (build & audit modes)';
