/**
 * TrustUpdater contract test suite — original location (M7-C).
 *
 * ⚠️  MOVED to storage layer for symmetry with FactReader (M8 Slice B):
 *     packages/eureka/src/storage/__tests__/trust-updater.contract.test.ts
 *
 * All 14 TrustUpdater contract tests (7 InMemory + 7 SQLite) run from that file.
 * This file is retained as a tombstone only — it holds no live tests.
 */

import { describe, it } from 'vitest';

describe('TrustUpdater contract suite — tombstone (suite moved)', () => {
  it.todo('suite moved to storage/__tests__/trust-updater.contract.test.ts — 7 InMemory + 7 SQLite tests live there');
});

