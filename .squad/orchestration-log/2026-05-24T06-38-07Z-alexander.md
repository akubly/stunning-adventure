# Alexander — Runtime/Loop Read (2026-05-24T06:38:07Z)

## Status
Completed (idle, 206s elapsed)

## Task
Read harness vision, surface runtime/loop questions

## Output Summary

**Vision interpreted as:**
- Self-improving CLI runtime with message loop, session recording, model selection
- Learning loop: sessions → Cairn primitives → Forge observations → Curator automation → Geneticist evolution
- Five primitives (request, artifact, observation, decision, question) form decision ledger with immutable hash-linked chain
- Replay and provenance tracing for audit and skill evolution

**Prior art:** ReAct (thought-action-observation), OpenHands (multi-agent orchestrator), LangGraph (DAG-based with stateful memory), AutoGen (turn-based conversation), Aider (approval gates and sandboxing), Claude SDK (state serialization and approval patterns).

**Eight runtime tensions identified:**

1. **Turn structure ambiguity:** Single LLM invocation or multi-step ReAct-style loop? Affects primitive granularity and replay fidelity.

2. **Tool execution ownership:** LLM decides (function-calling), Harness routes (dispatcher), or sub-agents delegate? Cascades to primitive recording hooks.

3. **Sub-agent spawning model:** Ephemeral (spawn→execute→terminate) or persistent (spawn→idle→reuse)? State inheritance vs. isolation?

4. **State persistence scope:** In-memory conversation buffer (write-behind) or write-ahead to Cairn? Affects replay correctness and ledger atomicity.

5. **Model routing trigger:** Per-session, per-turn, per-skill, or capability-based? Undefined dynamic routing behavior.

6. **Approval gate blocking:** Inline synchronous prompt, async queue, or confidence-threshold hybrid? Affects CLI UX and workflow orchestration.

7. **Primitive recording timing:** Write-ahead (intent), write-behind (outcome), or both? Unclear atomicity (turn vs. tool-call vs. decision).

8. **Turn atomicity for replay:** Atomic unit unclear — whole turn, single tool call, or single decision? Defines hash-chain checkpoint granularity.

## Questions Posed
Eight runtime-loop decision questions forwarded to Aaron, each with explicit rationale.

## Next Steps
Awaiting Aaron's responses to define execution semantics.
