# Orchestration Log: Crispin R6 Reconciliation

**Agent:** Crispin (Knowledge Representation Specialist)  
**Session:** R6 Source-Reading Reconciliation  
**Timestamp:** 2026-05-24T22:30:00Z  
**Duration:** ~2.5 hours (synthetic estimate)  
**Scope:** Reconcile cassima-requirements-r5-v3.md (PRD v3) knowledge representation design against actual Cairn/Forge code  

---

## Directive

R6 source-reading rule lift. Crispin tasked with deep schema reconciliation focusing on knowledge representation, fact model, decision schema, edges, and kind taxonomy. Output: actionable recommendations on Path A (clean-slate Eureka) vs Path B (Cairn extension).

## Findings Summary

### Schema Collisions Discovered

| Collision | Severity | Details |
|-----------|----------|---------|
| `kind=session` facts vs `sessions` table | CRITICAL | Cairn's sessions is first-class entity with 12 migration dependencies; v3's sessions-as-facts is incompatible by design |
| Structured `decide` schema vs flat `DecisionRecord` | HIGH | Forge uses `(question, chosenOption, alternatives[], confidence)` enum; v3 uses `(question, options[{id,label,rationale}], chosen, confidence 0-1)` |
| Per-tier `.db` files vs single `knowledge.db` | HIGH | Architectural mismatch; Cairn assumes monolithic DB; v3 assumes three-tier separation |
| Edges as first-class vs foreign keys | HIGH | v3 proposes Tier 1/2/3 edges; substrate has zero graph infrastructure, only relational FKs |

### Key Discoveries

1. **Vector support doesn't exist** — `change_vectors` is a misnomer (prescription learning deltas, not embeddings); no `sqlite-vec`
2. **No graph infrastructure** — zero relations table, zero edge types, zero traversal logic in Cairn or Forge
3. **Sessions are load-bearing** — Cairn's `sessions` table is referenced across 12 migrations; cannot be collapsed into fact store without full schema rewrite
4. **Kind taxonomy missing** — Crispin's six-kind epistemology (Practical/Semantic/Syntactic/Linguistic/Symbolic/Philosophical) is not represented in PRD v3's activity-oriented enum

### Verdict

**PRD v3 describes a new system, not an evolution of Cairn.** Two paths:

#### Path A: Clean-slate Eureka (RECOMMENDED)
- Build Eureka as standalone package (`packages/eureka/`) with own schema
- Storage: `~/.copilot/eureka/{agent,project,user}.db` with `sqlite-vec`
- Cairn unchanged; Eureka ingests Cairn's events but stores in fact-oriented schema
- v4 PRD rewrites FR-7.3: "Eureka does not reuse Cairn's database"

#### Path B: Cairn Extension (NOT RECOMMENDED)
- Sessions stay as table (not facts); decisions use Forge's shape
- Add edges as migration 013; vector support as migration 014
- Eureka becomes Cairn plugin; forces convergence on incompatible schemas

**Recommendation:** Path A. Cairn is observability-optimized; Eureka is knowledge-optimized. Forcing convergence creates a schema serving neither use case well.

---

## Next Steps

- [ ] Aaron chooses Path A or B
- [ ] Cassima incorporates decision into v4 PRD
- [ ] Edgar reviews learning-kernel extraction strategy
- [ ] Alexander stress-tests any refactoring

**Sign-off:** Crispin R6 reconciliation complete. Path A recommended.
