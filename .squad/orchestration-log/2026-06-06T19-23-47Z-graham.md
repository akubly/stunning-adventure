# Orchestration Log: Graham — Issue #31 Worktree Fallback Warning

**Timestamp:** 2026-06-06T19:23:47Z  
**Agent:** Graham (Lead / Architect)  
**Task:** Add user-visible warning for worktree fallback degradation  
**Issue:** #31  
**PR:** #53  
**Branch:** squad/31-worktree-fallback-warning  
**Outcome:** ✅ SUCCESS

## Summary

Updated `.github/agents/squad.agent.md` to emit user-visible warnings when worktree creation or dependency linking falls back to main checkout or standard npm install. Preserves existing log entries for auditability while surfacing degradation in real time. Rationale: silent degradation violates principle of least surprise when user opted into isolation via `SQUAD_WORKTREES=1`.

## Warnings Implemented

**Step 2(c) — worktree creation failure:**
```
⚠️  Worktree creation failed — falling back to main checkout. Isolation disabled for this spawn.
```

**Step 2(d) — dependency linking failure:**
```
⚠️  Worktree dependency linking failed — fell back to npm install. Dependency isolation is degraded for this spawn.
```

## Note

Coordinator restart required after merge to pick up governance file changes.
