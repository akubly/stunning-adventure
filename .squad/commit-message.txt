feat(forge): Phase 4 Export Pipeline — DBOM persistence + SKILL.md compiler

Adds the third integration seam: Export Pipeline (Extract → Strip → Attach → QualityGate).

New modules:
- packages/forge/src/export/ — pipeline orchestrator, SKILL.md compiler, stage functions, quality gate integration via dependency injection
- packages/cairn/src/db/dbomArtifacts.ts — DBOM persistence CRUD (migration 010)
- docs/forge-phase4-spec.md — architecture specification

Design constraints:
- Forge never imports @akubly/cairn (DI pattern for quality gate + persistence)
- All stages are pure functions: (StageContext) → StageContext
- Pipeline works offline from persisted events (no live SDK session required)
- Deterministic content hash (injected timestamp, consistent YAML escaping)

Persona-reviewed (5-persona Code Panel, squad mode):
- 8 findings accepted and addressed (2 blocking, 4 important, 2 minor)
- Key fixes: backslash YAML escaping, deterministic compiledAt injection, persistence gated on quality gate pass, stripStage dead code removal

Tests: 831 total (438 Cairn + 393 Forge), all passing.
Phase 4 adds 110 new tests (99 export + 11 DBOM persistence).

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
