feat(forge): Phase 4.5 — Local Feedback Loop (PGO engine)

Add telemetry, prescribers, and applier modules to Forge. Sessions
produce drift/token/outcome signals → collectors aggregate → prescribers
generate optimization hints → applier writes improved SKILL.md v2.

Telemetry:
- DriftCollector, TokenCollector, OutcomeCollector (HookObserver pattern)
- Streaming quantile sketch for accurate p50/p95 across sessions
- Individual signal components surfaced in ExecutionProfile
- LocalDBOMSink with fail-open logging and droppedCount counter

Prescribers:
- Prompt optimizer (convergence, tool entropy, context management)
- Token optimizer with determinism drift gate (>= 0.3 → zero hints)
- Shared buildSnapshot utility, classifyDriftLevel reuse

Applier:
- Order-stable optimization via (impactScore desc, id asc) sort
- Self-tuning strategy with adaptive explorationBudget
- tokenPressure normalized via budgetLimitNanoAiu

DB (Cairn):
- Migration 011: signal_samples, execution_profiles, optimization_hints
- CRUD modules with TTL sweep, row cap, upsert, status lifecycle

Shared types (@akubly/types):
- ExecutionProfile, ProfileGranularity, FeedbackSource
- OptimizationHint, StrategyParameters (open-shaped)

1012 tests passing (Forge 534 + Cairn 478). Review-hardened: 11 persona
review findings addressed (4 blocking, 4 important, 3 minor).

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
